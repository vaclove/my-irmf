{
  "name": "my-irmf-transcode-worker",
  "version": "1.0.0",
  "private": true,
  "description": "Out-of-process movie transcode worker (Azure Container Apps Job). Reads a job from a storage queue, transcodes the master to a 720p proxy with ffmpeg, uploads it to Google Drive.",
  "main": "index.js",
  "scripts": {
    "start": "node index.js"
  },
  "dependencies": {
    "@azure/storage-queue": "^12.0.0",
    "axios": "^1.10.0",
    "express": "^4.21.2",
    "googleapis": "^173.0.0",
    "pg": "^8.11.3"
  }
}
